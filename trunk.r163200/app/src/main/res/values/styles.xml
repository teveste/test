<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Header1_5">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="CustomTextView001">
        <item name="android:background">@drawable/custom_background_002</item>
        <item name="android:popupBackground">@color/custom_popup_background</item>
        <item name="android:textColor">@color/custom_text</item>
        <item name="android:textColorHint">@color/custom_hint</item>
    </style>
</resources>
